name: Citilab Kostroma Parser

on:
  workflow_dispatch:
  schedule:
    - cron: "35 3 * * 1"

jobs:
  citilab:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas openpyxl beautifulsoup4 lxml urllib3

      - name: Prepare folders
        run: |
          mkdir -p out
          mkdir -p output

      - name: Run Citilab parser
        run: |
          set -euxo pipefail
          python parsers/citilab_kostroma.py --outdir out

      - name: Debug output
        if: always()
        run: |
          echo "=== out ==="
          ls -lah out || true
          echo "=== output ==="
          ls -lah output || true

          python - << 'PY'
          from pathlib import Path
          import pandas as pd

          candidates = [
              Path("out/citilab_kostroma.csv"),
              Path("output/citilab_kostroma.csv"),
          ]

          found = False
          for csv_file in candidates:
              if csv_file.exists():
                  found = True
                  print(f"FOUND: {csv_file}")
                  try:
                      df = pd.read_csv(csv_file)
                      print(f"Rows: {len(df)}")
                      if len(df) > 0:
                          print(df.head(20).to_string(index=False))
                  except Exception as e:
                      print(f"CSV read error: {e}")

          if not found:
              print("No citilab CSV found")
          PY

      - name: Upload CSV artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: citilab-kostroma-csv
          path: |
            out/citilab_kostroma.csv
            output/citilab_kostroma.csv
          if-no-files-found: error

      - name: Upload XLSX artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: citilab-kostroma-xlsx
          path: |
            out/citilab_kostroma.xlsx
            output/citilab_kostroma.xlsx
          if-no-files-found: ignore
