name: Kislorod Ivanovo Parser

on:
  workflow_dispatch:
  schedule:
    - cron: "45 3 * * 1"

jobs:
  kislorod:
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas openpyxl lxml playwright

      - name: Install Playwright browser
        run: |
          python -m playwright install --with-deps chromium

      - name: Prepare folders
        run: |
          mkdir -p output
          mkdir -p out

      - name: Show parser file
        run: |
          ls -lah parsers
          python --version

      - name: Run Kislorod parser
        run: |
          set -euxo pipefail
          python parsers/kislorod_ivanovo.py || true

      - name: Debug output
        if: always()
        run: |
          echo "=== output ==="
          ls -lah output || true
          echo "=== out ==="
          ls -lah out || true

          python - << 'PY'
          from pathlib import Path
          import pandas as pd

          candidates = [
              Path("output/kislorod_ivanovo.csv"),
              Path("out/kislorod_ivanovo.csv"),
          ]

          found = False
          for csv_file in candidates:
              if csv_file.exists():
                  found = True
                  print(f"FOUND: {csv_file}")
                  try:
                      df = pd.read_csv(csv_file)
                      print(f"Rows: {len(df)}")
                      if len(df) > 0:
                          print(df.head(20).to_string(index=False))
                  except Exception as e:
                      print(f"CSV read error: {e}")

          if not found:
              print("No kislorod CSV found")
          PY

      - name: Upload CSV artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kislorod-ivanovo-csv
          path: |
            output/kislorod_ivanovo.csv
            out/kislorod_ivanovo.csv
          if-no-files-found: error

      - name: Upload XLSX artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kislorod-ivanovo-xlsx
          path: |
            output/kislorod_ivanovo.xlsx
            out/kislorod_ivanovo.xlsx
          if-no-files-found: ignore
