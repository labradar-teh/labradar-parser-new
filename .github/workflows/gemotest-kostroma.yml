name: Gemotest Kostroma Parser

on:
  workflow_dispatch:
  schedule:
    - cron: "30 3 * * 1"

jobs:
  gemotest:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pandas openpyxl lxml

      - name: Create output dir
        run: mkdir -p output

      - name: Run Gemotest parser
        run: |
          set -euxo pipefail
          python parsers/gemotest_kostroma.py

      - name: Debug output
        run: |
          echo "FILES:"
          ls -lah output || true

      - name: Upload CSV
        uses: actions/upload-artifact@v4
        with:
          name: gemotest-kostroma-csv
          path: output/gemotest_kostroma.csv
          if-no-files-found: error

      - name: Upload XLSX
        uses: actions/upload-artifact@v4
        with:
          name: gemotest-kostroma-xlsx
          path: output/gemotest_kostroma.xlsx
          if-no-files-found: error
